# GitHub Release Download

From the GitHub Release page, download the asset named like:
- `KRT-beta-0.6.0b.zip`
- `KRT-release-0.6.0R.zip`
- matching `.sha256` files for the same asset name
- `KRT-beta-0.6.0b.zip.sha256`

Do not use `Source code (zip)` or `Source code (tar.gz)`. Those are generated by GitHub and include
the whole repository.
The release ZIP still contains only the addon folder `!KRT/`.

## Release Channel Resolution

Release channel is derived from `!KRT/CHANGELOG.md` `Release-Version`:
- suffix `A` = alpha internal, no GitHub publication
- suffix `B` = beta prerelease
- suffix `R` = stable release

GitHub publishing starts from that changelog version metadata:
- pushing to `main` or `master` with a changed numeric version in `!KRT/CHANGELOG.md`
- or running the publish workflow manually

Only the numeric part triggers publication:
- `0.6.0b -> 0.6.0r` does not publish
- `0.6.0b -> 0.6.1r` publishes

## Optional Integrity Check

```powershell
Get-FileHash .\KRT-beta-0.6.0b.zip -Algorithm SHA256
```

Compare the hash value with the content of `KRT-beta-0.6.0b.zip.sha256`.
